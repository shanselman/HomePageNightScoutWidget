{
  "name": "homepage-nightscout-widget",
  "version": "1.0.0",
  "private": true,
  "description": "A read-only Nightscout adapter for Homepage's built-in Custom API widget.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "start:env": "node --env-file=.env src/server.js",
    "check": "node --check src/config.js && node --check src/glucose.js && node --check src/nightscout.js && node --check src/server.js",
    "test": "node --test test/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.js",
    "test:live": "node --test test/live.integration.js",
    "test:live:env": "node --env-file=.env --test test/live.integration.js"
  }
}
